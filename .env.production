# Vite-time environment variables for production builds.
#
# GA4 Measurement ID. Public identifier (ships in the bundle), so it's
# fine to commit. The consent banner gates `gtag` injection — analytics
# only loads after the user explicitly accepts.
VITE_GA4_MEASUREMENT_ID=G-FY186GC5SK
