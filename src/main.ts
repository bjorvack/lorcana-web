/**
 * App entry. Mounts <app-root> and wires the store.
 * TODO: implement components, store, and AI feature gating per DESIGN.md.
 */
import "./components/app-root.js";

document.addEventListener("DOMContentLoaded", () => {
  // placeholder
});
